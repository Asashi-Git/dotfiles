#include <stdio.h>

int main(){

    //2D array = an array, where each element is an entire array
    //          useful if you need a matrix, grid, or table of data

    //we can't declare a 2D array without assigning maximum 
    int numbers[2][3] = {{1, 2, 3},//here we have [2] array with [3] data
                         {4, 5, 6}};
    

    return 0;
}