#include <stdio.h>

int find_max(int x, int y){

    // if(x > y){
    //     return x;
    // }
    // else{
    //     return y;
    // }

    //to avoid all these line we can use an ternary operator
    return (x > y) ?
}