#include <stdio.h>

void sort(int array[], int size){
    
}

int main(){

    int array[] = {9, 1, 8, 2, 7, 3, 6, 4, 5};
    int size = sizeof(array)/sizeof(array[0]);

    sort(array, size);

    return 0;
}