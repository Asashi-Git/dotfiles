#include <stdio.h>

int main(){

    //constant = fixed value that cannot be altered by the program during its execution

    float pii = 3.14159;

    pii = 420.69;

    printf("%f\n", pii); //here with a float we can change the value of the variable

    //to prevent that we use contant

    const PI / 3.14159;

    //PI = 420.69;//if we trie to asign a different value, we will get an error

    printf("%f\n", PI);
    //Good practice is to declare a constant vire upercase

    return 0;
}