#include <stdio.h>
#include <math.h>//we need to include math to have some math function

int main(){

    double A = sqrt(9);
    double B = pow(2, 4);

    printf("\n%lf\n",A);
    printf("\n%lf\n",B);

    return 0;
}